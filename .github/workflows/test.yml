name: Test
on: push

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Use Node.js
        uses: actions/setup-node@v2
        with:
          node-version: 14.x
          check-latest: true

      - name: Checkout code
        uses: actions/checkout@v2

      - run: npm i -g pnpm

      - run: pnpm i --frozen-lockfile

      # 环境变量 CI 默认为 true
      - name: Test
        run: pnpm run test
